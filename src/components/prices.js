import * as React from "react";

const Prices = () => {
    return (
        <div
            style={{
                margin: `0 auto`,
                maxWidth: `var(--size-content)`,
                padding: `var(--size-gutter)`,
            }}
        >
            <p className="lg:text-4xl md:text-2xl sm:text-[1.4rem] font-['gabdugi'] text-[#DA6A00] font-semibold  md:mb-4 md:font-bold ">Комплексные пакеты услуг</p>
        </div>
    )
}

export default Prices;
